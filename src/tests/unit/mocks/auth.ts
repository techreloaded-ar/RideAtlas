// src/tests/unit/mocks/auth.ts
// Mock per @/auth

// Sostituisce il modulo auth per i test
export const auth = jest.fn();
export const signIn = jest.fn();
export const signOut = jest.fn();
export const handlers = {};
